# Copie para .env e preencha. O arquivo .env está no .gitignore e nunca vai para o Git.

# Caminho do perfil do Chrome usado pela automação (padrão: ./.chrome-profile)
BROWSER_FLOW_PROFILE=

# Credenciais de sites que seus fluxos acessem — use ${VAR} dentro dos JSONs
SITE_USER=
SITE_PASSWORD=
